# fly.toml app configuration file for DHAKAN Discord Bot on Fly.io
# Background worker process with persistent SQLite volume (no public HTTP listeners)

app = "dhakan-discord-bot"
primary_region = "sin"

[build]

[env]
  NODE_ENV = "production"
  DB_PATH = "/data/database.sqlite"

[mounts]
  source = "dhakan_data"
  destination = "/data"
  initial_size = "1gb"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory = "512mb"
  auto_stop_machines = false
  auto_start_machines = false
